import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.theworkforceapp.thisrupt',
  appName: 'The Workforce Wpp',
  webDir: 'dist',
  server: {
    androidScheme: 'https'
  }
};

export default config;
